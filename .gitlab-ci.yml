image: navigator

stages:
  - checkout
  - build
  - test
  - format

ckeckout:
  stage: checkout
  script:
  - sh $CATKIN_DIR/src/NaviGator/scripts/CI/checkout.sh

build:
  stage: build
  script:
  - sh $CATKIN_DIR/src/NaviGator/scripts/CI/build.sh

test:
  stage: test
  script:
  - sh $CATKIN_DIR/src/NaviGator/scripts/CI/test.sh

format:
  stage: format
  script:
  - sh $CATKIN_DIR/src/NaviGator/scripts/CI/format.sh